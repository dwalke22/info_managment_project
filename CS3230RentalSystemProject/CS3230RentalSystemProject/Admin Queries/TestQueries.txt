Test Query 1:

select m.memberID, concat(m.firstName, ' ', m.lastName) as customerFullName, i.rentalDate, i.dueDate, sum(i.quantity) as totalItems from member m, rentaltransaction r, rentalitem i where m.memberID = r.memberID and i.rentalID = r.transactionID and ((select count(rentali.furnitureID) from `rentaltransaction` rental, `rentalitem` rentali where rental.transactionID = rentali.rentalID and rental.memberID = r.memberID and rentali.rentalDate = i.rentalDate) >= 2) GROUP by m.memberID, i.rentalDate, i.dueDate order by m.memberID, i.rentalDate

Test Query 2

select m.memberID, concat(m.firstName, ' ', m.lastName) as customerFullName, i.rentalDate, i.dueDate, sum(i.quantity) as totalItems from member m, rentaltransaction r, rentalitem i where m.memberID = r.memberID and i.rentalID = r.transactionID and ((select count(rentali.furnitureID) from `rentaltransaction` rental, `rentalitem` rentali where rental.transactionID = rentali.rentalID and rental.memberID = r.memberID and rentali.rentalDate = i.rentalDate) >= 2) GROUP by m.memberID, i.rentalDate, i.dueDate order by m.memberID, i.rentalDate
